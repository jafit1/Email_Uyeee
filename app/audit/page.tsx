"use client"; import { useEffect,useState } from "react"; import Link from "next/link"; type L={id:string;action:string;targetId?:string;createdAt:string}; export default function Audit(){const [items,setItems]=useState<L[]>([]);useEffect(()=>{fetch("/api/audit-logs").then(r=>r.json()).then(setItems)},[]);return <main><Link href="/dashboard">Kembali</Link><h1 className="text-2xl font-bold my-6">Audit Log</h1><section className="card">{items.map(x=><p key={x.id}>{new Date(x.createdAt).toLocaleString()} · {x.action}</p>)}</section></main>}
