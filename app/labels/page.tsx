"use client"; import { useEffect,useState } from "react"; import Link from "next/link"; type L={id:string;name:string;color:string;_count:{accounts:number}};
export default function Labels(){const [items,setItems]=useState<L[]>([]);const load=()=>fetch("/api/labels").then(r=>r.json()).then(setItems);useEffect(()=>{load()},[]);async function add(f:FormData){await fetch("/api/labels",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),color:f.get("color")})});load()}return <main><Link href="/dashboard">Kembali</Link><h1 className="text-2xl font-bold my-6">Label</h1><form action={add} className="flex gap-2"><input name="name" placeholder="Nama label" required/><input name="color" type="color" defaultValue="#64748b"/><button>Tambah</button></form><section className="card mt-4">{items.map(x=><p key={x.id}><span style={{color:x.color}}>●</span> {x.name} ({x._count.accounts})</p>)}</section></main>}
