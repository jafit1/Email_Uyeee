"use client"; import { useRouter } from "next/navigation"; import Link from "next/link";
export default function New(){const r=useRouter();async function submit(f:FormData){const body=Object.fromEntries(f);const res=await fetch("/api/emails",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...body,labelIds:[]})});if(res.ok)r.push("/emails");}return <main><Link href="/emails">Kembali</Link><h1 className="text-2xl font-bold my-6">Tambah Akun</h1><form action={submit} className="card space-y-3"><input name="email" type="email" placeholder="Email" required/><input name="alias" placeholder="Nama/alias"/><input name="provider" placeholder="Provider"/><input name="loginUrl" type="url" placeholder="URL login"/><input name="username" placeholder="Username"/><input name="password" type="password" placeholder="Password opsional"/><input name="twoFactorType" placeholder="Tipe 2FA"/><input name="totpSecret" placeholder="TOTP secret opsional"/><textarea name="recoveryCodes" placeholder="Recovery codes opsional"/><textarea name="notes" placeholder="Catatan privat opsional"/><button>Simpan</button></form></main>}
