import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { requireUser,audit } from "@/lib/auth"; import { labelInput } from "@/lib/validation";
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireUser(),id=(await params).id,v=labelInput.parse(await req.json()),r=await db.label.updateMany({where:{id,userId:u.id},data:v});if(!r.count)return NextResponse.json({error:"Not found"},{status:404});await audit(u.id,"LABEL_UPDATED",id);return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"Invalid label"},{status:400});}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const u=await requireUser(),id=(await params).id,r=await db.label.deleteMany({where:{id,userId:u.id}});if(!r.count)return NextResponse.json({error:"Not found"},{status:404});await audit(u.id,"LABEL_DELETED",id);return new NextResponse(null,{status:204});}catch{return NextResponse.json({error:"Unauthorized"},{status:401});}}
