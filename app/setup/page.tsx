"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export default function Setup(){const [error,setError]=useState(""),router=useRouter();async function submit(f:FormData){const password=String(f.get("password"));if(password!==f.get("confirm"))return setError("Password tidak sama.");const r=await fetch("/api/auth/setup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password})});if(r.ok)router.push("/dashboard");else setError("Setup gagal. Gunakan password minimal 12 karakter.");}return <main className="max-w-md"><section className="card"><h1 className="text-2xl font-bold mb-2">Setup Vault</h1><p className="text-slate-400 mb-6">Buat akun master pertama.</p><form action={submit} className="space-y-3"><input name="email" type="email" placeholder="Email master" required/><input name="password" type="password" minLength={12} placeholder="Master password, min. 12 karakter" required/><input name="confirm" type="password" placeholder="Ulangi password" required/><button className="w-full">Buat vault</button></form>{error&&<p className="text-red-300 mt-3">{error}</p>}</section></main>}
