"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export default function Login(){const [error,setError]=useState(""),router=useRouter();async function submit(f:FormData){const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.ok)router.push("/dashboard");else setError("Login gagal atau terkunci.");}return <main className="max-w-md"><section className="card"><h1 className="text-2xl font-bold mb-6">Personal Email Vault</h1><form action={submit} className="space-y-3"><input name="email" type="email" placeholder="Email master" required/><input name="password" type="password" placeholder="Master password" required/><button className="w-full">Login</button></form>{error&&<p className="text-red-300 mt-3">{error}</p>}</section></main>}
