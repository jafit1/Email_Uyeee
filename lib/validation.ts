import { z } from "zod";
export const accountInput = z.object({ email: z.string().email().max(320), alias: z.string().max(120).optional().nullable(), provider: z.string().max(120).optional().nullable(), loginUrl: z.string().url().max(2048).refine(value => !value || /^https?:\/\//i.test(value), "HTTP(S) URL required").optional().nullable(), username: z.string().max(320).optional().nullable(), password: z.string().max(4096).optional().nullable(), twoFactorType: z.string().max(80).optional().nullable(), totpSecret: z.string().max(512).optional().nullable(), recoveryCodes: z.string().max(8192).optional().nullable(), notes: z.string().max(16384).optional().nullable(), labelIds: z.array(z.string().cuid()).max(30).default([]) });
export const labelInput = z.object({ name: z.string().trim().min(1).max(50), color: z.string().regex(/^#[0-9a-fA-F]{6}$/) });
